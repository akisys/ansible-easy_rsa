---
# tasks file for easy_rsa (Ubuntu specific)

- name: Install easy-rsa packages
  become: yes
  apt:
    name: "{{ easy_rsa_packages }}"
    state: present
    update_cache: yes
  tags:
    - easy_rsa
